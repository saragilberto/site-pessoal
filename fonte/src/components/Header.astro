---
import type { SiteContent } from '@/data/site'

interface Props {
  nav: SiteContent['nav']
  /**
   * Prefixo das ancoras. Vazio na home; nas paginas de texto precisa ser
   * absoluto ('/en/'), senao '#work' aponta para uma secao que nao existe ali.
   */
  base?: string
}

const { nav, base = '' } = Astro.props
const links = [
  { href: `${base}#work`, label: nav.work },
  { href: `${base}#platform`, label: nav.platform },
  { href: `${base}#stack`, label: nav.stack },
  { href: '/writing/', label: nav.writing },
  { href: `${base}#about`, label: nav.about },
  { href: `${base}#contact`, label: nav.contact },
]
---

<header
  class="sticky top-0 z-30 border-b border-ink/5 dark:border-paper/10 bg-paper/80 dark:bg-ink/80 backdrop-blur"
>
  <div class="container-prose flex h-16 items-center justify-between">
    <a href={nav.switchHref === '/' ? '/en/' : '/'} class="font-serif text-lg tracking-tight">
      sara<span class="text-accent">.</span>
    </a>

    <nav class="hidden md:flex items-center gap-8 text-sm">
      {links.map((link) => <a href={link.href} class="link-underline">{link.label}</a>)}
    </nav>

    <a
      href={nav.switchHref}
      class="font-mono text-xs uppercase tracking-[0.2em] text-ink/60 dark:text-paper/60 hover:text-accent transition-colors"
    >
      {nav.switchLabel}
    </a>
  </div>
</header>
